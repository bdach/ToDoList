<UserControl
    x:Class="Ticketron.App.Views.TaskGroupNavigationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:Ticketron.App.ViewModels"
    mc:Ignorable="d">
    <UserControl.DataContext>
        <viewModels:TaskGroupNavigationViewModel x:Name="ViewModel" />
    </UserControl.DataContext>

    <NavigationView
        Loaded="OnNavigationViewLoaded"
        MenuItemsSource="{x:Bind ViewModel.TaskGroups, Mode=OneWay}">
        <NavigationView.MenuItemTemplate>
            <DataTemplate x:DataType="viewModels:TaskGroupViewModel">
                <NavigationViewItem
                    Content="{x:Bind Name}">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="{x:Bind Icon}" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </DataTemplate>
        </NavigationView.MenuItemTemplate>
        <Frame />
    </NavigationView>
</UserControl>
